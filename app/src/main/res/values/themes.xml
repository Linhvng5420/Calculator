<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Calculator2024" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowFullscreen">true</item>
        <item name="actionBarStyle">@style/MyActionBarStyle</item>
    </style>

    <style name="MyActionBarStyle" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="titleTextStyle">@style/MyActionBarTitleStyle</item>
        <item name="background">@drawable/actionbar_background</item>
    </style>

    <style name="MyActionBarTitleStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Theme.Calculator2024" parent="Base.Theme.Calculator2024" />

    <style name="MyStyleButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">2dp</item>
        <item name="cornerRadius">5dp</item>
        <item name="android:gravity">center</item>
        <item name="android:backgroundTint">#65E91E63</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:autoSizeMaxTextSize">50sp</item>
        <item name="android:autoSizeTextType">uniform</item>
    </style>
</resources>